<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="splash_screen" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Parámetros para Android 12+ (API 31) -->
        <item name="android:windowSplashScreenBackground">@color/primary_book</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        
        <!-- Retrocompatibilidad para versiones anteriores -->
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:statusBarColor">@color/primary_book</item>
        <item name="android:navigationBarColor">@color/primary_book</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>
</resources>